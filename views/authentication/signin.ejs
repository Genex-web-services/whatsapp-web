<!-- meta tags and other links -->
<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>
<style>
    body {
        overflow-y: hidden;
    }
</style>

<body class="dark:bg-neutral-800 bg-neutral-100 dark:text-white">

    <section class="bg-white dark:bg-dark-2 flex flex-wrap min-h-[100vh]">
        <div class="lg:w-1/2 lg:block hidden">
            <div class="flex items-center flex-col h-full justify-center">
                <img src="<%= env.Assets_URL%>/images/auth/auth-img.png" alt="">
            </div>
        </div>
        <div class="lg:w-1/2 py-3 mt-0 px-3 flex flex-col justify-center">
            <div class="lg:max-w-[464px] mx-auto w-full">
                <div>
                    <a href="/index" class="mb-2.5 text-center">
                        <img class="text-center" src="<%= env.Assets_URL%>/logo.png" width="200px" alt="">
                    </a>
                    <h4 class="mb-3">Sign In to your Account</h4>
                    <p class="mb-8 text-secondary-light text-lg">Welcome back! please enter your detail</p>
                </div>
                 <!-- Email login button instead of input -->
    <div class="mb-6">
      <a href="<%= env.Login_URL%>/login/?clientid=b696c1bc-a76b-4abd-8c91-918fd139a2ef&redirect_url=https://admin.gws365.in/auth/callback&status=logging"
         class="block w-full text-center py-4 rounded-xl border border-neutral-300 bg-neutral-50 dark:bg-dark-2 text-base font-medium hover:bg-primary-50 transition">
         Login with Email ID
      </a>
    </div>

    <div class="mb-8 text-center relative before:absolute before:w-full before:h-[1px] before:top-1/2 before:-translate-y-1/2 before:bg-neutral-300 before:start-0">
      <span class="bg-white dark:bg-dark-2 relative z-10 px-4">Or sign in with</span>
    </div>

    <div class="flex flex-col gap-4">
      <!-- Google Login -->
      <a href="<%= env.Login_URL%>/login/?clientid=5bfeb894-ecb4-4d88-a153-d1c38bb71c00&redirect_url=https://auth.gws365.in/auth/callback&status=logging"
         class="flex items-center justify-center gap-3 w-full py-4 rounded-xl border border-neutral-300 text-neutral-600 dark:text-neutral-200 font-semibold hover:bg-primary-50 transition">
        <iconify-icon icon="logos:google-icon" class="text-primary-600 text-xl"></iconify-icon>
        Login with Google
      </a>

      <!-- Microsoft Login -->
      <a href="<%= env.Login_URL%>/login/?clientid=5bfeb894-ecb4-4d88-a153-d1c38bb71c00&redirect_url=https://auth.gws365.in/auth/callback&status=logging"
         class="flex items-center justify-center gap-3 w-full py-4 rounded-xl border border-neutral-300 text-neutral-600 dark:text-neutral-200 font-semibold hover:bg-primary-50 transition">
        <iconify-icon icon="logos:microsoft" class="text-primary-600 text-xl"></iconify-icon>
        Login with Microsoft
      </a>
    </div>

            </div>
        </div>
    </section>

    <%- include('../partials/script') %>
    <script>
        // ================== Password Show Hide Js Start ==========
        function initializePasswordToggle(toggleSelector) {
            $(toggleSelector).on("click", function() {
                $(this).toggleClass("ri-eye-off-line");
                var input = $($(this).attr("data-toggle"));
                if (input.attr("type") === "password") {
                    input.attr("type", "text");
                } else {
                    input.attr("type", "password");
                }
            });
        }
        // Call the function
        initializePasswordToggle(".toggle-password");
        // ========================= Password Show Hide Js End ===========================
    </script>
                    

</body>
</html>
